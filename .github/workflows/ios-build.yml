name: iOS Build

on:
  push:
    branches:
      - master

jobs:
  build-ios:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Yarn
        run: npm install -g yarn

      - name: Install Dependencies
        run: yarn install

      - name: Install CocoaPods
        run: |
          sudo gem install cocoapods
          cd ios
          pod install

      - name: Build iOS App
        run: |
          cd ios
<<<<<<< HEAD
          xcodebuild -workspace MyCareBridge.xcworkspace -scheme MyCareBridge -sdk iphonesimulator -configuration Release
=======
          xcodebuild -workspace MyCareBridge.xcworkspace -scheme MyCareBridge -sdk iphoneos -configuration Release
>>>>>>> 90a4033f755308580471d062ecb69cfb8b82a019

      - name: Upload Build Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ios-build
          path: ios/build/Release-iphoneos/
